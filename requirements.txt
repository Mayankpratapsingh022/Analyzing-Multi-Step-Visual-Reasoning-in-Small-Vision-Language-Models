# Python dependencies for small-vlm-reasoning
# PyTorch, torchvision, and CUDA are pre-installed on RunPod base images.
# Install with: pip install -r requirements.txt

# HuggingFace ecosystem
transformers>=4.44.0
accelerate>=0.33.0
bitsandbytes>=0.43.0
datasets>=2.20.0
huggingface_hub>=0.24.0

# Vision / model utilities
timm>=1.0.3
Pillow>=10.3.0
einops>=0.8.0
qwen-vl-utils>=0.0.2

# Data science
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.13.0
scikit-learn>=1.5.0

# Plotting & reporting
matplotlib>=3.9.0
seaborn>=0.13.0

# Experiment tracking
wandb>=0.17.0

# Utilities
tqdm>=4.66.0
pyyaml>=6.0

# API model support
openai>=1.35.0
anthropic>=0.30.0

# Flash-attention is NOT listed here — it must be installed separately with
# the pre-built wheel matching your exact CUDA + PyTorch versions.
# Run this after pip install -r requirements.txt:
#
#   pip install flash-attn --no-build-isolation
#
# Or use the setup script flag (handles this automatically):
#   bash scripts/setup_runpod.sh  (without --skip-flash-attn)
